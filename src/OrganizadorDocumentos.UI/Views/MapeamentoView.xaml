<UserControl x:Class="OrganizadorDocumentos.UI.Views.MapeamentoView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" d:DesignHeight="600" d:DesignWidth="800">

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="20">
            <TextBlock Text="Mapeamento da Estrutura" Style="{StaticResource HeaderTextStyle}"/>

            <StackPanel Orientation="Horizontal" Margin="0,0,0,20">
                <Button Content="[ Atualizar estrutura ]" Command="{Binding AtualizarEstruturaCommand}"
                        Style="{StaticResource ActionButtonStyle}" IsEnabled="{Binding Mapeando, Converter={StaticResource InverseBooleanConverter}}"/>
                <TextBlock Text="{Binding StatusMapeamento}" VerticalAlignment="Center" Margin="15,0,0,0"
                           Foreground="#7F8C8D"/>
            </StackPanel>

            <ProgressBar IsIndeterminate="{Binding Mapeando}" Height="5" Margin="0,0,0,20"
                         Visibility="{Binding Mapeando, Converter={StaticResource BooleanToVisibilityConverter}}"/>

            <Grid Margin="0,0,0,20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Border Grid.Column="0" Style="{StaticResource CardStyle}">
                    <StackPanel>
                        <TextBlock Text="Colaboradores" Style="{StaticResource LabelTextStyle}"/>
                        <TextBlock Text="{Binding TotalColaboradores}" FontSize="36" FontWeight="Bold"
                                   Foreground="#3498DB"/>
                    </StackPanel>
                </Border>

                <Border Grid.Column="1" Style="{StaticResource CardStyle}">
                    <StackPanel>
                        <TextBlock Text="Anos" Style="{StaticResource LabelTextStyle}"/>
                        <TextBlock Text="{Binding TotalAnos}" FontSize="36" FontWeight="Bold"
                                   Foreground="#9B59B6"/>
                    </StackPanel>
                </Border>

                <Border Grid.Column="2" Style="{StaticResource CardStyle}">
                    <StackPanel>
                        <TextBlock Text="Pastas Mensais" Style="{StaticResource LabelTextStyle}"/>
                        <TextBlock Text="{Binding TotalPastasMensais}" FontSize="36" FontWeight="Bold"
                                   Foreground="#1ABC9C"/>
                    </StackPanel>
                </Border>
            </Grid>

            <Border Style="{StaticResource CardStyle}">
                <StackPanel>
                    <TextBlock Text="Colaboradores Mapeados" FontSize="16" FontWeight="Bold"
                               Foreground="#2C3E50" Margin="0,0,0,10"/>
                    <ListView ItemsSource="{Binding Colaboradores}" MaxHeight="300"
                              ScrollViewer.VerticalScrollBarVisibility="Auto">
                        <ListView.View>
                            <GridView>
                                <GridViewColumn Header="Pasta" DisplayMemberBinding="{Binding NomePasta}" Width="200"/>
                                <GridViewColumn Header="Nome Normalizado" DisplayMemberBinding="{Binding NomeNormalizado}" Width="200"/>
                                <GridViewColumn Header="Anos" DisplayMemberBinding="{Binding Anos.Count}" Width="80"/>
                            </GridView>
                        </ListView.View>
                    </ListView>
                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</UserControl>
